//! host-core — OS-agnostic host orchestration shared by per-OS host shells.
//! Phase-0a: intentionally thin. The host logic lives in `host-windows` until a
//! second host OS forces extraction (concrete-before-trait, ADR-0001).
